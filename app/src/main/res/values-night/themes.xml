<!-- res/values/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.MyApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Màu chính -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorSecondary">@color/teal_700</item>
        <item name="titleTextColor">#fff</item>
        <item name="descriptionTextColor">#CCCACA</item>

        <!-- Màu nền -->
        <item name="android:colorBackground">#121212</item>
        <item name="colorOnBackground">#FFFFFF</item>

        <!-- Áp dụng bo góc -->
        <item name="shapeAppearanceSmallComponent">@style/RoundedCorner12</item>
    </style>

    <style name="RoundedCorner12" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerSize">12dp</item>
    </style>
</resources>
